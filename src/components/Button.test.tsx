import React from "react";
import { render } from "@testing-library/react";

import Button from "./Button";

describe('Button', () => 1{
    test('render the button component', () => {
        render(: any<Button: any>hello world</Button:>)
    });
})